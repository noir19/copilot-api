# 2026-04-09 Settings Placeholder Panel Review

## 范围

- 调整 dashboard 设置页的预留区域
- 不再使用单一小块文本占位
- 改成更接近真实后台的只读配置卡片

## 改动

- 扩大设置页可视区域
- 新增四类占位配置卡片：
  - Token 与认证
  - 日志与 SQLite
  - 异步队列
  - 导出与排障
- 所有控件仍为只读假数据，但结构已贴近后续真实配置页面
- 增加较长说明文案区域，避免文本显示不全

## 验证

- `bun run lint`
- `bun run typecheck`
- `bun run build`

## 备注

- 这次只改页面表现，没有引入新的后端接口
- 真正可写的配置目前仍只有模型别名和展示映射
